@set("css", css ?? "")

@if(capitalize)
	@set("css", css + " capitalize")
@end

@if(bold)
	@set("css", css + " font-bold")
@end
<span 
	class="text-xs {{css}}"
>
	@if(value)
		{{value}}
	@else 
		{{{ await $slots.main()}}}
	@end
</span>
