@set("extraCss", extraCss ?? "")
@set("background", background ?? "bg-accent-100")
@set("as", as ?? "div")
    <{{as}} class="{{background}} flex-1 flex flex-col p-4 rounded-lg gap-4 {{extraCss}}" {{$props.serializeExcept(['extraCss', 'as', "background"])}}>
		{{{ await $slots.main() }}}
</{{as}}>

