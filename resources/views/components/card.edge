@set("extraCss", extraCss ?? "")
@set("as", as ?? "div")
    <{{as}} class="bg-background flex-1 flex flex-col p-4 rounded-lg gap-4 {{extraCss}}" {{$props.serializeExcept(['extraCss', 'as'])}}>
		{{{ await $slots.main() }}}
</{{as}}>

